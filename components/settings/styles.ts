import { tv } from "tailwind-variants";

export const settingsPageTitle = tv({
  base: "text-base font-medium text-default-700",
});

export const settingsPageSubtitle = tv({
  base: "mt-1 text-sm font-normal text-default-400",
});

export const cardStyles = tv({
  base: "mt-4 border border-default-200 bg-transparent",
});
